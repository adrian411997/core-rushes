import { StyledContainer } from "./Form.styles";
import { Inputs } from "./inputs/Inputs";

export const Form = () => {
  return (
    <StyledContainer>
      <Inputs />
    </StyledContainer>
  );
};
